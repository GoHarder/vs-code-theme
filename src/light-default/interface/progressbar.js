import { tokens } from '../colors/interface.js';

const progressBar = {
  // Background color of the progress bar shown for long running operations.
  // {default: #0e70c0}
  background: tokens.primary,
};

export default { 'progressBar.background': progressBar.background };
